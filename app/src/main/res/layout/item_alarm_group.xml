<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/group_holder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:foreground="@drawable/selector"
    android:paddingHorizontal="@dimen/activity_margin"
    android:paddingVertical="@dimen/medium_margin">

    <org.fossify.commons.views.MyTextView
        android:id="@+id/group_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="@dimen/bigger_text_size"
        app:layout_constraintEnd_toStartOf="@id/group_switch"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Trabajo" />

    <org.fossify.commons.views.MyTextView
        android:id="@+id/group_alarm_count"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:alpha="0.7"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="@dimen/smaller_text_size"
        app:layout_constraintEnd_toStartOf="@id/group_switch"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/group_title"
        tools:text="3 alarmas" />

    <org.fossify.commons.views.MyMaterialSwitch
        android:id="@+id/group_switch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="@dimen/normal_margin"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/group_rename"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/group_rename"
        android:layout_width="@dimen/drag_handle_size"
        android:layout_height="@dimen/drag_handle_size"
        android:padding="@dimen/tiny_margin"
        android:src="@drawable/ic_edit_vector"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/group_delete"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/group_delete"
        android:layout_width="@dimen/drag_handle_size"
        android:layout_height="@dimen/drag_handle_size"
        android:padding="@dimen/tiny_margin"
        android:src="@drawable/ic_delete_vector"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
